#include <iostream>

using namespace std;


// ลอง loop แต่ละ N แล้วหาว่า ถ้าเปลี่ยนกระเบื้องตัวที่ i ไปเป็น 1-100 แล้วตัวไหน cost ดีสุด
// โดยถ้า ให้ได้ตัวที่ k จะได้ dp[m][n] =
// เก็บ dp[M][N]
int dp[10001][11];
int Tile[11];
int main()
{
    int n, m;
    cin >> n >> m;
    for(int i = 1; i <= n; i++){
        cin >> Tile[i];
    }

    for(int i = )


    return 0;
}
